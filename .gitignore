# Electron runtime / deps (huge — regenerate with `npm install` in desktop/)
desktop/node_modules/
node_modules/
/test-output/

# Vercel local project link
.vercel

# Packaging artifacts
*.zip

# macOS
.DS_Store
